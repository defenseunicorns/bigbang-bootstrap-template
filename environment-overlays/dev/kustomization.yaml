resources:
  - ../../bigbang-base
  - ../../apps/pod-info
  - ../../apps/dependency-track
  - flux-system

configMapGenerator:
  - name: environment
    namespace: bigbang
    behavior: merge
    files:
      - values.yaml=values.yaml

patchesStrategicMerge:
  - environment-bb-secret.enc.yaml
